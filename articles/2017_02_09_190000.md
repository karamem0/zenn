---
title: "SPClient 0.4 を公開しました"
emoji: "🔔"
type: "tech"
topics: ["news"]
published: true
---

:::message
この記事ははてなブログに 2017/2/9 に投稿したものの転載です。
:::

https://www.powershellgallery.com/packages/SPClient/0.4

権限設定に関連する関数を追加しました。

こんな感じで書けます。

```powershell
Add-SPClientType

Connect-SPClientContext -Online `
    -Url "{{site-url}}" `
    -UserName "{{user-name}}" `
    -Password (ConvertTo-SecureString -String "{{password}}" -AsPlainText -Force)

$user = Get-SPClientUser -Name "{{user-name}}"
$list = Get-SPClientList -Title "Site Pages"
$list | Enable-SPClientUniqueRoleAssignments
$list | Add-SPClientRoleAssignments -Member $user -Roles "Full Control"
```

権限設定はもともとが面倒なつくりになっていて苦労するのですが、この方法だとかなり直観的にできると思います。
